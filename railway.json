{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["docs/**"]
  },
  "deploy": {
    "startCommand": "cd docs && python3 -m http.server ${PORT:-8000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
